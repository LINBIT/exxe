TESTS = \
	all \
	onexit \
	prefix \
	python \
	quoting_and_variables \
	timeout

EXTRA_DIST = \
	$(TESTS) \
	test-lib.sh

TESTS_ENVIRONMENT = \
	abs_top_builddir=$(abs_top_builddir); \
	export abs_top_builddir;

LOG_COMPILER = \
	$(SHELL)
